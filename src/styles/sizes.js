const sizes = [0.75, 1, 1.25, 1.5, 2];
const sizeNames = {
  small: `${sizes[0]}rem`,
  normal: `${sizes[1]}rem`,
  medium: `${sizes[2]}rem`,
  big: `${sizes[3]}rem`,
  extraBig: `${sizes[4]}rem`,
};

export default sizeNames;